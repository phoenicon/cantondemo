# Screenshots

Real evidence only — capture these from your own terminal / the explorer and drop
them in here, then reference them from the README or report. Don't ship placeholders.

| File | What to capture |
|------|-----------------|
| `jwt-success.png` | `python3 src/canton_lab.py token` printing the token prefix + `(ok)` |
| `party-created.png` | the `internal party: farmfort::1220…` line |
| `acs-empty.png` | `acs` returning `[]` (the correct pre-airdrop read) |
| `holdings.png` | `balance` showing Holding UTXOs after the airdrop lands |
| `explorer-tx.png` | the party/preapproval transaction in the public Canton explorer |

Tip on macOS: `Cmd+Shift+4` then drag to grab a region of the terminal.
